

/**
 * Analyse les réponses et retourne un score AI.
 * @param {Array} responses - Réponses de l'étudiant
 * @returns {Promise<number>} - Score calculé
 */
export const analyzeResponsesWithAI = async (responses) => {
 
    console.log("AI will be processing...")

  return "AI treatment";
};
